# Legacy Go implementation

The original Go version of WSL Tray, kept for reference. The maintained
implementation is the Rust crate at the repository root.

Build: `.\build.ps1` (needs Go 1.22+). The mask generator lives in
`tools/gentux` and writes `tuxmask.go`.
